import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  allText: {
    fontSize: 18,
    textAlign: 'center',
  },
  text: {
    fontSize: 18,
    color: '#51582F',
  },
});
export default styles;
