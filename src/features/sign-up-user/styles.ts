import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    form: {
        alignItems: "center",
        paddingTop: 30,
        width: "100%",
        gap: 20,
      },
})
export default styles