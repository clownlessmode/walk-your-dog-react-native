import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    conditionsText: {
        fontSize: 16,
        textAlign: 'center',
        maxWidth: 307,
      },
})
export default styles